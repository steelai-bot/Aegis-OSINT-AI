# Minimal dependencies for Aegis OSINT AI (Windows-first)
# Install: .venv\Scripts\python.exe -m pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic-settings>=2.5.0

# Database
aiosqlite>=0.20.0

# Templates
jinja2>=3.1.4
python-multipart>=0.0.9

# Development dependencies (optional, for testing & linting)
# pip install pytest pytest-asyncio respx mypy ruff