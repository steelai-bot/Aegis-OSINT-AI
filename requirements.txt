# ══════════════════════════════════════════════════════════════
#  AU-OSINT-RECON — Python Dependencies
#  Install: pip install -r requirements.txt
#  Python 3.10+ required
# ══════════════════════════════════════════════════════════════

# ── Core HTTP & Networking ────────────────────────────────────
requests>=2.31.0
httpx>=0.27.0
aiohttp>=3.9.0
urllib3>=2.2.0
PySocks>=1.7.1                  # SOCKS5 proxy support (Tor)
stem>=1.8.2                     # Tor controller

# ── HTML / Web Scraping ───────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.1.0
playwright>=1.43.0              # Headless browser (JS-heavy targets)
selenium>=4.20.0
webdriver-manager>=4.0.1

# ── PDF Extraction ────────────────────────────────────────────
PyMuPDF>=1.24.0                 # Primary PDF engine (fitz)
pypdf>=4.2.0                    # Fallback PDF engine
pdfminer.six>=20221105          # Text extraction fallback
pytesseract>=0.3.10             # OCR for scanned PDFs (requires tesseract-ocr system pkg)
Pillow>=10.3.0                  # Image processing for OCR

# ── Telegram ─────────────────────────────────────────────────
telethon>=1.36.0                # Telegram MTProto client

# ── Data Processing ───────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
python-dateutil>=2.9.0
chardet>=5.2.0                  # Encoding detection for combo lists
charset-normalizer>=3.3.0

# ── Cryptography & Hashing ────────────────────────────────────
cryptography>=42.0.0
passlib>=1.7.4                  # Hash identification
bcrypt>=4.1.0
pyOpenSSL>=24.0.0

# ── Archive Handling ──────────────────────────────────────────
py7zr>=0.21.0                   # 7zip support for stealer archives
rarfile>=4.1                    # RAR support
zipfile38>=0.0.3                # Extended zip support

# ── Report Generation ─────────────────────────────────────────
jinja2>=3.1.4                   # HTML template rendering
weasyprint>=62.0                # HTML → PDF export
reportlab>=4.2.0                # PDF generation
openpyxl>=3.1.2                 # Excel export
tabulate>=0.9.0                 # CLI table formatting

# ── Environment & Config ──────────────────────────────────────
python-dotenv>=1.0.1
click>=8.1.7                    # CLI framework
rich>=13.7.0                    # Rich terminal output
tqdm>=4.66.0                    # Progress bars

# ── Async & Concurrency ───────────────────────────────────────
asyncio-throttle>=1.0.2
aiofiles>=23.2.1
tenacity>=8.3.0                 # Retry logic with backoff

# ── OSINT Utilities ───────────────────────────────────────────
dnspython>=2.6.0                # DNS lookups
python-whois>=0.9.4             # WHOIS queries
ipwhois>=1.3.0                  # IP/ASN lookup
shodan>=1.31.0                  # Shodan API (optional)
censys>=2.2.9                   # Censys API (optional)

# ── Exploit Scanner ───────────────────────────────────────────
pycurl>=7.45.3                  # Low-level HTTP for timing attacks
sslyze>=6.0.0                   # SSL/TLS analysis

# ── AI / HuggingFace ─────────────────────────────────────────
transformers>=4.41.0            # HuggingFace Transformers
tokenizers>=0.19.0              # Fast tokenizers
huggingface-hub>=0.23.0         # Model download & hub API
accelerate>=0.30.0              # Multi-device inference acceleration
bitsandbytes>=0.43.0            # 4-bit / 8-bit quantisation (GGUF alt)
sentencepiece>=0.2.0            # Tokenizer for LLaMA/Mistral models
protobuf>=4.25.0                # Required by some tokenizers
safetensors>=0.4.3              # Safe model weight loading

# ── GGUF / llama.cpp (CPU-first inference) ────────────────────
llama-cpp-python>=0.2.77        # llama.cpp Python bindings (CPU/Metal/CUDA)
# Install with CPU-only (default, no CUDA required):
#   pip install llama-cpp-python
# Install with CUDA support (optional):
#   CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python

# ── Hardware Detection ────────────────────────────────────────
psutil>=5.9.8                   # CPU, RAM, disk info
py-cpuinfo>=9.0.0               # Detailed CPU info
GPUtil>=1.4.0                   # GPU detection (optional, graceful fallback)

# ── Logging & Monitoring ──────────────────────────────────────
loguru>=0.7.2                   # Structured logging

# ── Testing ───────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
