# Development / CI dependencies
# Install together with runtime deps: pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
respx>=0.22.0
ruff>=0.6.0
mypy>=1.11.0
